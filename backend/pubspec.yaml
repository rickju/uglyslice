name: ugly_slice_backend
description: Cloud Run backend for ugly_slice — parses Overpass data and returns course JSON.
publish_to: 'none'

environment:
  sdk: ^3.0.0

dependencies:
  ugly_slice_shared:
    path: ../shared
  latlong2: ^0.9.1
  dart_jts: ^0.3.0+1
  collection: ^1.19.1
  http: ^1.6.0
  crypto: ^3.0.0
  functions_framework: ^0.4.0
  sqflite_common_ffi: ^2.3.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.21.0
